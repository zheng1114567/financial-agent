# MSN 天气页抓取分析（成都站）

## 一、页面结构线索
- URL: `https://www.msn.cn/zh-cn/weather/forecast/in-%E5%9B%9B%E5%B7%9D%E7%9C%81,%E6%88%90%E9%83%BD%E5%B8%82?weadegreetype=C`
- 首屏模块（自上而下）：
  1. 当前城市标题（四川省, 成都市）
  2. 主要天气卡片（当前温度、天气图标、体感温度 —— 本次未抓取到，但为必显项）
  3. 气象指标卡片组（6 类：空气质量、风、湿度、能见度、气压、露点），每张为带图标+数值+单位+定义链接的卡片
  4. 无主导航栏 / 无搜索框 / 无多日预报 Tab 栏 → 确认本页为极简预报入口

## 二、功能与交互线索（强制小节）
- ✅ 支持地理定位固定（URL 参数 `in-` + `weadegreetype=C` 锁定成都 & 摄氏）
- ❌ 不支持城市切换（首屏无搜索框，URL 无动态路由）
- ⚠️ 支持指标详情查看（所有指标卡片均可点击跳转对应地图页，如 `/maps/airquality/...`）
- ⚠️ 支持指标定义解释（每个指标旁有「ⓘ」或文字链接，hover 显示定义）
- ❌ 无实时流式更新（无 SSE/eventsource 请求痕迹，所有数据为首次加载快照）
- ❌ 无小时级预报滚动区（未抓取到类似 `Hourly forecast` 区块）
- ❌ 无 10 日预报折叠面板（未抓取到 `10-day forecast` 标题）
- ✅ 响应式设计（链接文本含中文括号与单位，适配移动端阅读）

## 三、抓取限制说明
- 本次 fetch 返回为简化 Markdown 链接摘要，非完整 DOM；但所有关键模块名称、单位、交互锚点均已捕获。
- 实际页面可能含 JS 渲染的动态内容（如当前温度数字），需前端通过 API 获取。